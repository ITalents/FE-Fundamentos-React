const idade = 21;
const podeDrigir = idade >= 18 ? "sim" : "não"

console.log(`Pode dirigir ? ${podeDrigir}`);

const numero = 10;
// const resultado = numero % 2 === 0 ? "Par" : "Impar";
console.log(`O resultado é ${numero % 2 === 0 ? "Par" : "Impar"}`);